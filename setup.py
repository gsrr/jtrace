import traceback
import sys
import os
from distutils.core import setup

setup(
    name="jtrace",
    version='0.1',
    license='open',
    long_description=open('README').read(),
    packages = ['jtrace']
 )
